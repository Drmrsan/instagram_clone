#error_explanation
	- if obj.errors.any?
		.alert.alert-danger 
			%h4.alert-heading The form contains #{pluralize(obj.errors.count, "error")}.
			%ul
				-obj.errors.full_messages.each do |msg|
					%li
						%p #{msg}